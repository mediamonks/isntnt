{
  "name": "isntnt",
  "version": "1.0.0",
  "description": "TypeScript runtime type guards",
  "main": "dist/index.js",
  "author": "Nathan Uphoff",
  "license": "MIT",
  "scripts": {
    "start": "yarn && yarn watch",
    "watch": "tsc -w -d --skipLibCheck",
    "build": "jest -u --collectCoverage && rimraf dist && tsc -d --skipLibCheck",
    "test": "jest --watchAll --coverage",
    "clear_jest": "jest --clearCache"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "jest": "^24.9.0",
    "standard": "^14.0.2",
    "ts-jest": "^24.0.2",
    "typescript": "^3.5.2"
  }
}
